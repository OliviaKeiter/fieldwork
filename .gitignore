node_modules/
dist/
.astro/
.netlify/
*.log
.env
.DS_Store

# Supabase CLI local state. `supabase init` writes config.toml and `supabase link`
# writes .temp/, both of which carry your own project ref. Neither belongs in a
# fork's history.
supabase/.temp/
supabase/config.toml
