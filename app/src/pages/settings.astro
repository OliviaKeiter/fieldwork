---
import AppShell from '../layouts/AppShell.astro';
import SettingsForm from '../components/SettingsForm';
---

<AppShell
  title="Settings"
  active="settings"
  description="Profile, timing rules, models, and whimsy. Every fact the app uses, in one place."
>
  <SettingsForm client:load />
</AppShell>
